import React from 'react';

const EmptyNote = () => (
  <div className="mt-4 border rounded-lg w-full shadow p-3 h-40 flex items-center justify-center">
    <h1 className="text-2xl fond-bold text-center text-gray-500">There are no todos here!</h1>
  </div>
);

export default EmptyNote;
